type Participant {
  address: String!
  shares: Token
}

type Token {
  name: String
  symbol: String!
  amount: String!
  decimals: Int
}

# progress
type Organisation {
  address: String! # yes
  platform: String! # yes
  name: String! # yes
  txid: String! #yes
  timestamp: Int #yes
  blockNumber: Int #yes
  totalSupply: Token! #yes
  shareValue(symbol: String!): Token! #yes
  bank: [Token] # yes
  participants: [Participant] # yes
  participant(address: String!): Participant # yes
}

type Account {
  address: String!
  organisations: [Organisation]
}

type Query {
  account(address: String!): Account # nope
  organisation(address: String): Organisation # progress
}
