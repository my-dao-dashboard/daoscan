type Participant {
  address: String!
  shares: Token
}

type Token {
  name: String!
  amount: String!
  decimals: Int
}

# progress
type Organisation {
  address: String! # yes
  platform: String! # yes
  name: String! # yes
  shares: Token! # yes
  bank: [Token]
  participant(address: String!): Participant
  participants: [Participant] # partial
  txid: String! #yes
  timestamp: Int #yes
  blockNumber: Int #yes
}

type OrganisationsPaged {
  items: [Organisation]
  cursor: String
}

type Account {
  address: String!
  organisations: [Organisation]
}

type Query {
  account(address: String!): Account # nope
  organisation(address: String): Organisation # progress
}
